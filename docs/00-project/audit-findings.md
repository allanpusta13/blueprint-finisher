Total issues found: 15

| # | Location | Issue Description |
|---|---|---|
| 1 | Section 2 — Table 6 (`stock_movements`) / Section 6.2 (`InventoryService::adjustment`) | In Section 2 (Table 6, line 840), `reference_code` is defined as nullable (`Yes`). However, when `adjustment()` is called in `InventoryService` (line 3381), `reference_code` is left completely undefined/null, resulting in unreferenced/blank reference code values for all adjustment ledger entries in `StockMovementsTable`. |
| 2 | Section 7A.2 — `ProductsTable.php` (lines 3950–4001) | The `ProductsTable` card schema displays `sku` and `name` (Variant) and `product.name` (Family), but completely omits the `barcode` field defined on `product_variants` (Section 2, Table 2, line 770) and present in `ProductForm` (line 3874) and `ProductInfolist` (line 4086). |
| 3 | Section 7C.3 — `DirectTransfersTable.php` (lines 4924–4986) | The `DirectTransfersTable` labels its `reference_code` column as `'REFERENCE'` (line 4938) instead of following the `'REFERENCE CODE'` naming convention used in `TransferRequisitionInfolist` (line 4620), `PurchaseOrderInfolist` (line 5828), and `SalesOrderInfolist` (line 6322). |
| 4 | Section 7C.3 — `DirectTransfersTable.php` (lines 4975–4980) | The `DirectTransfersTable` includes a `warehouse_id` filter (line 4976) but completely omits a `product_variant_id` / SKU filter or search filter for product variants, unlike `StockMovementsTable` (line 5225) and `LossLedgersTable`. |
| 5 | Section 7D.2 — `InTransitsTable.php` (lines 5092–5094) | The `InTransitsTable` filters only by `status` (line 5093), omitting both `warehouse_id` and `product_variant_id` / SKU filters, despite displaying `transferRequisition.reference_code` and `productVariant.sku`. |
| 6 | Section 7E.1 & 7E.2 — `StockMovementInfolist.php` & `StockMovementsTable.php` (lines 5143, 5209) | Both `StockMovementInfolist` (line 5143) and `StockMovementsTable` (line 5209) label `reference_code` as `'REFERENCE'` rather than `'REFERENCE CODE'`, inconsistent with document infolists (`TransferRequisitionInfolist` line 4620, `PurchaseOrderInfolist` line 5828, `SalesOrderInfolist` line 6322). |
| 7 | Section 7B.4 — `TransferRequisitionInfolist.php` (line 4661) | In `TransferRequisitionInfolist`, line items label `productVariant.sku` as `'ORIGINAL SKU'` (line 4661) when a substitute variant exists, but the substitute product variant's SKU (`substituteProductVariant.sku`) is missing from the infolist entry. |
| 8 | Section 2 — Tables 14 & 15 (`suppliers` & `customers`) (lines 972–998) | Master data entities `suppliers` (Table 14) and `customers` (Table 15) do not define an entity `code` column in Section 2 or in their respective forms/tables, unlike `warehouses` (Table 5, line 820) which defines a unique `code` field (e.g. `WH-CEBU-01`). [uncertain] |
| 9 | Section 7C.2 — `CreateDirectTransfer.php` (lines 4899–4902) | The `CreateDirectTransfer` page generates a `$referenceCode` ('DT-...') and queries `StockMovement` by `where('reference_code', $referenceCode)`, but because `directTransfer()` creates two movement records (out and in) with the exact same `reference_code`, `firstOrFail()` arbitrarily selects the `TransferOut` record without linking both movements in a dedicated document record. |
| 10 | Section 0 — Physical-to-Digital State Lifecycle Diagram (line 46) vs Section 6.2 (`InventoryService::scanToReceive`) | The Section 0 state lifecycle diagram (line 46) specifies `completed / closed_with_loss / cancelled` as terminal states. However, in `InventoryService::scanToReceive()` (lines 3318–3323), when cargo write-offs occur during scan-to-receive, the requisition status is set only to `Completed` or `PartiallyReceived`. No code logic exists in `InventoryService` or `NegotiationService` to ever transition a `TransferRequisition` to `TransferRequisitionStatus::ClosedWithLoss`. |
| 11 | Section 10 — Bento Grid ASCII Diagram (lines 8075–8090) vs Widget Definitions Table (line 8120) | In the Section 10 Bento Grid ASCII diagram (lines 8075–8079), `StatsOverview` is rendered as a top-row stat summary component spanning 2 columns. However, in the Widget Definitions table (line 8120), `StatsOverviewWidget`'s Type is incorrectly specified as `TableWidget` instead of a stat card widget type. |
| 12 | Section 10 — Bento Grid ASCII Diagram (lines 8085–8087) vs Dashboard Registration Array (lines 8184–8192) | In the Section 10 Bento Grid ASCII diagram (lines 8085–8087), Row 3 consists of `Sales vs Purchases` (2 cols), `Top Selling Variants` (1 col), and `Pending Fulfillment` (1 col). In the Dashboard Registration array (lines 8189–8191), the registration order matches, but in the Widget Definitions table (line 8127), `PendingFulfillmentWidget` data source is defined as count of pending SO/PO, whereas its representation in the ASCII diagram is labeled `Pending Fulfillment` without specifying whether it renders as a table or stat card. |
| 13 | Section 7D.1 & 7D.2 — `InTransitInfolist.php` (line 5034) vs `InTransitsTable.php` (lines 5060–5091) | The `InTransitInfolist` schema (line 5034) includes `cleared_at` ('CLEARED AT') as a displayed field, but `InTransitsTable` (lines 5060–5091) omits the `cleared_at` timestamp column entirely from the table view. |
| 14 | Section 7B.4 — `TransferRequisitionInfolist.php` (line 4650) vs Section 1D.1 / 1D.2 Icon Standards | In `TransferRequisitionInfolist` (line 4650), `receivedBy.name` entry carries `Heroicon::QrCode` as its icon, whereas all other sign-off user entries (`requestedBy`, `approvedBy`, `dispatchedBy` in lines 4633–4635) use user/badge/truck icons, and `PurchaseOrderInfolist` (line 5849) uses `Heroicon::ArchiveBoxArrowDown` for `receivedBy.name`. |
| 15 | Section 7H.4 — `SalesOrderInfolist.php` (lines 6333–6334) vs `SalesOrder` Model / `PurchaseOrderInfolist.php` (lines 5850–5851) | In `SalesOrderInfolist` (lines 6333–6334), the SIGN-OFFS section displays `confirmed_at` and `dispatched_at` but omits `ordered_at` (defined on the `SalesOrder` model in line 2007), whereas `PurchaseOrderInfolist` (lines 5850–5851) displays `ordered_at`. |
